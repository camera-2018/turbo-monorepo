{
  "name": "new-monorepo",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "workspaces": [
    "frontend"
  ],
  "packageManager": "yarn@1.22.19",
  "private": true,
  "scripts": {
    "dev": "npx turbo dev",
    "lint": "npx turbo lint:fix",
    "prepare": "husky install",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint:strict": "turbo run lint:strict",
    "typecheck": "turbo run typecheck",
    "format:check": "turbo run format:check"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.5.0",
    "turbo": "^1.7.0"
  }
}
